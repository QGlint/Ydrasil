#ifndef YDRASIL_FINSH_CONFIG_H
#define YDRASIL_FINSH_CONFIG_H

/* RT-Thread Nano 3.1.5 expects this header; Ydrasil keeps the settings in
 * the shared BSP rtconfig.h so Nano and the retained 5.2.2 build agree. */
#include <rtconfig.h>

#endif

# 测试覆盖与优化方向

## 一、优先补齐的合规测试

### P0：官方接口和最终判定

1. 按 PDF 制作官方兼容包装层，在未经修改的官方顶层模板中跑通。
2. 将 IROM 调整为官方要求的 16KB、当前周期读出的分布式 ROM。
3. 将内部 4 位字节使能正确转换为官方 2 位 `perip_mask` 编码，并验证不同访问宽度。
4. 修改 testbench：只有全部阶段灯按顺序完成、数码管显示 37、最终总通过灯满足预期时才判定 PASS；同时设置明确的失败灯判据。
5. 用完整官方 `irom.coe + dram.coe` 运行，保存 LED、数码管、周期数、毫秒数、日志和波形。

### P1：指令、CSR、异常和 MMIO 覆盖

1. 初始化并运行仓库中的 `riscv-tests` 子模块，不能只依赖自定义测试。
2. 覆盖全部 RV32I、RV32M 及候选 Z 扩展指令的立即数、边界值、反向操作和数据相关场景。
3. 增加六种 Zicsr 指令 × 四个要求 CSR × 有效/零操作数的组合覆盖。
4. 验证 `ecall` 的 `mepc`、`mcause`、`mtvec`、MIE/MPIE 变化，以及 `mret` 后的恢复路径。
5. 增加 MMIO 边界测试：非 4 字节访问、非对齐访问、SW/KEY 读写限制、LED 只写、连续读写及读写周期时序。
6. 增加计数器启动、停止、复位、最大值、跨时钟域稳定性及连续命令测试。
7. 做一次“现场限时加入指定 Z 指令”的完整演练，包括编码、RTL 修改、测试、综合和上板。

### P2：性能和工程复现

1. 补充可复现的矩阵乘、CRC 和大规模排序测试，并记录周期数、毫秒数和吞吐率。
2. 让 CoreMark 连续运行至少 10 秒后再记录有效成绩。
3. 修复备用构建流程的源码顺序、重复 testbench 和脚本 CRLF 问题，保证换环境后可以一条命令复现。
4. 每次测试保存提交 SHA、工具版本、COE 文件哈希、仿真参数和结果摘要。

## 二、推荐的覆盖闭环

当前历史记录显示约 65.0% 行覆盖率、65.9% 翻转覆盖率、87.1% 分支覆盖率、79.7% 表达式覆盖率。下一轮不应只追求总体数字，而应针对以下空洞逐项关闭：

- 指令类别 × 操作数边界 × 数据相关距离；
- load-use、乘除法、多周期执行、分支预测失败和异常返回的交叉场景；
- 字节/半字/字访问及跨边界地址；
- CSR 读、写、只读和零操作数语义；
- MMIO 访问宽度、对齐、连续事务和读写时序；
- 复位、异常、`mret`、计数器命令之间的状态转换。

每个覆盖项都应绑定至少一个可复现测试和一个明确的 PASS 判据。

## 三、性能优化优先级

长测数据显示约 24.8% 周期消耗在 scoreboard 等待，建议按以下顺序处理：

1. 优化 load-use 转发、生产者唤醒和 scoreboard 释放；
2. 优化 LSU 热数据局部性，当前估算命中率约 64.5%；
3. 暂不优先修改分支预测器，当前分支预测准确率约 98.74%；
4. 将 IROM 收敛到官方 4096×32 深度和规定存储器类型，降低资源和接口风险；
5. 在 Vivado 中完成 50/150/200MHz 三档验证，保存 WNS、TNS、保持时序、CDC、资源和功耗报告。

## 四、完成标准

建议将“全部满足”定义为以下条件同时成立：

- 官方顶层接口与 PDF 完全一致；
- 初赛和决赛要求的全部指令、CSR、异常、MMIO 和性能测试均有日志；
- 官方 COE 在仿真和目标 FPGA 上均按顺序完成所有阶段并点亮总通过灯；
- CoreMark 等性能结果满足有效运行时长要求；
- Vivado 时序、资源、CDC 和功耗报告均通过；
- 初赛通知要求的代码、报告、演示材料和视频等提交物齐全。

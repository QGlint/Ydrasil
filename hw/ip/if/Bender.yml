package:
  name: ydrasil::if
  description: IF stage IP for Ydrasil RV32I core

dependencies: {}

sources:
  - target: any(verilator, iverilog, vcs)
    files:
      - rtl/if.sv

  - target: ip_tb
    files:
      - dv/if_tb.sv
